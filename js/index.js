window.onload = function (){
    const formID = document.getElementById("form-sign-in")
    const formTag = document.getElementsByTagName("form")
    const formClass = document.getElementsByClassName("form")

    const classSelector = document.querySelector(".input")
    const idSelector = document.querySelector("#id")
    const tagSelector = document.querySelector("input")

    const tagMultiSelector = document.querySelectorAll("input")
    const classMultiSelector = document.querySelectorAll(".input")


}